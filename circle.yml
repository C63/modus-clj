machine:
  services:
    - docker

dependencies:
  override:
    - lein with-profile dev_sb uberjar
    - docker info
    - docker build --rm=false -t buiduytung/modus-monolith-clj:1.0 .

deployment:
  hub:
    branch: master
    commands:
      - docker login --username=buiduytung --password=metropolia2012
      - docker push buiduytung/modus-monolith-clj:1.0
